# Examples

No runnable examples yet — `agentic-sidecar` is at the scaffold stage (see
[../ROADMAP.md](../ROADMAP.md)). The first example lands alongside v0.1
(Sidecar Runtime + Rule-Based Decision Gate), matching the pattern used in
the other DeepAgentLabs packages: one runnable script per shipped
capability, added in the same PR as the code it demonstrates.
